# Review, 2026-09-20

Three independent reviews of the codebase at commit 7fc8d72, run as separate agents following the cite-and-verify method (every finding carries a file and line, and was read before being reported; CONFIRMED means traced, PLAUSIBLE means likely but not fully traced). Axes are not merged or re-ranked against each other.

Tests at review time: kmdn-core 48 passed, spikes 11 passed, TypeScript clean.

## Security

| # | Sev | Status | Finding | Where |
|---|---|---|---|---|
| S1 | critical | confirmed | Unsanitized `marked` HTML rendered with `dangerouslySetInnerHTML`, CSP is `null`, and every Tauri command is reachable from the webview. A doc or PR body with `<img onerror>` can call `read_document` on the secrets file, `save_document` anywhere, or start an agent in Developer mode. | `RenderedMarkdown.tsx:20-28`, `rdiff/render.ts:12`, `tauri.conf.json:22-24` |
| S2 | high | confirmed | `Policy::path_allowed` does not normalize `..`. `/kb/assets/../../../home/u/.bashrc` matches `assets/**`; `docs/../AGENTS.md` bypasses the protected-file check; macOS case-insensitivity lets `agents.md` through. Claude deny rules, the pi gate, and Codex `workspace-write` do not catch it. | `agents/mod.rs:116-125`, `pi_gate.ts:29` |
| S3 | high | confirmed | No accept-or-revert step for agent edits (D15). Agent writes are swept into the human's next `Update <title>` commit with no trailer. | `lib.rs save_document`, `commit.rs:28-66` |
| S4 | medium | confirmed | Permission decisions fail open when paths are unknown: Codex approval with a missing item id and pi confirms that do not match the message pattern produce `paths: []`, which `decide` allows. | `codex.rs:227-247`, `pi.rs:173-179`, `mod.rs:135-138` |
| S5 | medium | confirmed | Tauri commands accept absolute and `..` paths: `read_document`, `save_document`, `clone_kb` and `create_kb` `dest`. Harmless from the app's own UI, but it is what turns S1 into token theft. | `lib.rs` those commands |
| S6 | medium | confirmed | GitLab marker approvals count any commenter, including the MR author. Misleading count only; GitLab still gates the merge. | `gitlab.rs:319-336` |
| S7 | medium | plausible | Agents launch with the worktree as cwd, so repo-controlled `.claude/settings.json`, `.pi/extensions`, `.codex/` from a collaborator run on launch. | `agents.rs:137` |
| S8 | low | confirmed | Agent log posted to the PR without preview; `post_agent_log` config is never read. | `lib.rs:355-358` |
| S9 | low | confirmed | Secret file written with umask permissions then chmod'ed; follows a pre-existing symlink. | `secrets.rs:58-68` |

Checked and fine: tokens only in headers, never in errors or agent env; remote host parsing strips userinfo so a hostile remote cannot receive the github.com token; slugs resolve through git's worktree list; GitLab paths are URL-encoded; push with lease checks the tracking ref; CI exposes the e2e secret only on schedule and dispatch.

## Performance

Measured with a bench over the real `rdiff` sources on the dev box.

| # | Impact | Status | Finding | Cost | Where |
|---|---|---|---|---|---|
| P1 | high | measured | Block alignment is O(n·m) and regex-normalizes inside the comparator; word diff has no size guard. | 2,000-line doc with one word changed: 487 ms. 50 files of 300 lines: 969 ms on the main thread. 400-line wrapped paragraph: 1.4 s and 1 GB. | `rdiff/align.ts:11,33`, `wordDiff.ts:7-22` |
| P2 | high | confirmed | `list_documents`, `run_checks`, `list_threads`, `thread_changes`, `local_changes`, `save_document` are sync commands, so they run on the Tauri main thread despite the file's claim. `index::scan` reads every file. | 5,000 docs: 0.3 to 0.6 s warm, seconds cold, UI frozen. Breaks D33 "tree renders immediately". | `lib.rs:257-337` |
| P3 | high | confirmed | Polling fan-out: `local_changes` every 5 s runs `git status` over the 1 GB clone; each sync tick invalidates docs, threads, changes, reviews; thread view rescans the worktree after every save. | Two full scans plus 12 statuses per minute at 5,000 docs. | `Sidebar.tsx:33-37,66`, `ThreadView.tsx:39` |
| P4 | high | confirmed | Provider traffic: open PRs fetched with one serial request per PR for files; two independent 60 s timers; no ETags or GraphQL despite D31. | 20 PRs: 42 requests per minute idle, 2,500 to 3,500 per hour against a 5,000 limit. | `github.rs:215-227`, `gitlab.rs:245-257`, `Sidebar.tsx:37,67` |
| P5 | medium | confirmed | Submit and checks read every document four times: index, scan for checks, re-read plus parse per file, stale-index scan. | 5,000 docs: 20,000 reads, 1.5 to 3 s warm. | `submit.rs:133-140`, `checks.rs:97-169` |
| P6 | medium | plausible | Sidebar renders 5,000 document rows unvirtualized; palette rebuilds entries per keystroke because mutation objects sit in `useMemo` deps. | 30 to 80 ms per navigation. | `Sidebar.tsx:163-170`, `CommandPalette.tsx:37-49` |
| P7 | low | fine | Sync loop git work: one fetch per tick, up-to-date rebases short-circuit. | ~5 ms per thread. | `sync.rs:163-165` |
| P8 | low | fine | Editor serializes the doc three times per keystroke for frontmatter detection. | ~0.3 ms at 2,000 lines. | `livePreview.ts:72` |
| P9 | low | refuted | Bundle: language modes already lazy; entry is 298 kB gzipped. Lazy-load the editor later. | | `vite.config.ts` |
| P10 | low | fine | One Tauri event per text delta. | 1 to 6 percent of a core while streaming. | `agents.rs:265-272` |

Priority: P1, P2, P4, P3, P5, P6.

## Code review

### Standards axis

| # | Sev | Smell | Finding | Where |
|---|---|---|---|---|
| C1 | high | Duplicated Code | Eight commands repeat `Repo::open`, `remote_info`, `provider_for`. | `lib.rs` eight sites |
| C2 | high | Duplicated Code | `s`, `os`, `check`, `get`, `send`, `all_pages` identical between GitHub and GitLab providers. | `github.rs:45-114`, `gitlab.rs:47-118` |
| C3 | high | Duplicated Code | `thread_changes` and `changes_between` share 40 lines; `let _ = base_commit` is dead. | `diff.rs` |
| C4 | medium | Repeated Switches | "Is this host GitHub" decided four different ways. | `lib.rs:83-99,247-251,879-885` |
| C5 | medium | Data Clumps | `submit_thread` rebuilds a fake `AppState` to call `author_for`. | `lib.rs:328-338` |
| C6 | medium | Duplicated Code | Noreply email guess in three places, hardcoded to github.com in `author_for` even for GitLab. | `lib.rs:112,232,903` |
| C7 | medium | Dead code | `else if false`, `let _ = ToolKind::Read`, `Options_`, boxed checkout builder. | `sync.rs:224`, `agents.rs:420`, `checks.rs:40`, `lib.rs:453` |
| C8 | medium | Primitive Obsession | Thread status string computed three ways in three components. | `ThreadView.tsx:76`, `Sidebar.tsx:143`, `ReviewView.tsx:78` |
| C9 | low | Shotgun Surgery | Hand-mirrored TS types; externally tagged sync enums force `"Ok" in outcome` checks. | `api.ts`, `Sidebar.tsx:30` |
| C10 | low | Stale comments | lib.rs doc says providers "land in later issues"; `running: true` hardcoded. | `lib.rs:18`, `agents.rs:392` |
| C11 | low | Style | 20+ lines over 160 chars; `window.prompt` and `window.confirm` instead of the Radix stack. | `AgentPanel.tsx:132`, `ThreadView.tsx:64,98` |

### Spec axis

| # | Sev | Decision | Finding |
|---|---|---|---|
| R1 | critical | D15, 06-agents, 10-ui | Accept-or-revert for agent edits does not exist. Same as S3. |
| R2 | critical | D9 | SSH remotes are not rewritten to HTTPS for fetch and push. Only clone uses `https_url`. Every sync and submit on a `git@` clone fails with an auth error. |
| R3 | high | 06-agents | Agent commits should be `Agent (<name>): <request>` with a `Co-Authored-By` trailer. Not implemented. |
| R4 | high | D26 | Agent-generated title and summary at submit not implemented; deterministic fallback exists. |
| R5 | high | D58 | Author cannot preview or drop the agent log before it is posted. |
| R6 | high | 05-git sync | A clean rebase is not force-pushed, so the open PR shows a stale head until the next submit. |
| R7 | high | 05-git publish, D49 | No remote branch cleanup after merge, no Done group, no 7-day worktree cleanup. |
| R8 | high | D27, D12 | No crash-recovery mirror of unsaved text; no SQLite anywhere. |
| R9 | medium | 05-git never-list | `save_document` writes any UI-supplied path. Same as S5. |
| R10 | medium | 06-agents | Mid-path `..` traversal in the agent policy. Same as S2. |
| R11 | medium | 10-ui | Thread header shows two of six states; overflow menu has Abandon only; Home has two groups of six. |
| R12 | medium | 06-agents | Home composer text becomes the slug and is discarded; the mode toggle is never sent. |
| R13 | medium | D10 | Tokens in a 0600 file, not the OS keychain. Documented interim. |
| R14 | medium | D31, 10-ui | No conditional requests or GraphQL; sidebar reviews not ordered by "requested from me". |
| R15 | low | D22 | One failed `pull_files` call hides every PR. |
| R16 | low | 05-git identity | Profile display name discarded; login used as commit name. |
| R17 | low | D57 | Unprotected-main hint never shown. |

Worst per axis: C1 on standards, R1 on spec. The reviewers declined to rank one axis above the other.

## What happens next

Issues opened for S1, S2, S4, S5, S6, S9 and P1 to P5 and R2 to R8, R12, R15, R16 (see the `review-2026-09-20` label). Existing issues cover R1 (#41), R4 (#42), R13 (#14), R14 (#12), C9 (#20), R11 (#24). S7 and C1 to C8 are folded into a refactor issue.
