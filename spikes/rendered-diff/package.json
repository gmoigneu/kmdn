{
  "name": "@kmdn/spike-rendered-diff",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": { "test": "vitest run", "lint": "tsc --noEmit -p tsconfig.json" },
  "dependencies": { "marked": "^15" },
  "devDependencies": { "typescript": "^5", "vitest": "^3" }
}
